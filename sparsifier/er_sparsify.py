

def er_sparsify(drop_rate, size):
    """
    Input:
        drop_rate: float, between 0 and 1
        size: int, number of elements to sparsify
        
    Output:
        edge_selection: tensor, shape (size), type torch.bool
    """
    pass